{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "cleanup-trials": "npx tsx scripts/cleanup-trials.ts",
    "cleanup-trials-cron": "npx tsx scripts/cleanup-trials.ts > logs/cleanup-$(date +%Y%m%d).log 2>&1"
  }
}